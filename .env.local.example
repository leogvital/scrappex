# Copie este arquivo para .env.local e preencha os valores reais.
# .env.local fica de fora do git (veja .gitignore) — só existe na sua máquina/servidor.

SCRAPPERX_APP_USER=admin
SCRAPPERX_APP_PASS=troque-esta-senha
